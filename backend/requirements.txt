# API-image dependencies (Docker target `api`).
# Browser-only packages (playwright, Pillow, pixelmatch) live in
# requirements-browser.txt and are installed ONLY in the `browser` target.
Django>=5.1,<6.0
djangorestframework>=3.15,<4.0
djangorestframework-simplejwt>=5.3,<6.0
django-cors-headers>=4.4,<5.0
celery[redis]>=5.4,<6.0
psycopg[binary]>=3.2,<4.0
python-dotenv>=1.0,<2.0
httpx>=0.27,<1.0
beautifulsoup4>=4.12,<5.0

django-anymail>=12.0,<14.0

# SaaS / enterprise: billing, encryption, diagnostics
stripe>=8.0,<12
cryptography>=41,<46
redis>=5.0,<7.0
reportlab>=4.0,<5.0

# Artifact object storage (S3 / S3-compatible / MinIO)
boto3>=1.28,<2

# Production serving
gunicorn>=22.0,<24.0
